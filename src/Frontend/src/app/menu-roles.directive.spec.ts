import { MenuRolesDirective } from './menu-roles.directive';

describe('MenuRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuRolesDirective();
    expect(directive).toBeTruthy();
  });
});
